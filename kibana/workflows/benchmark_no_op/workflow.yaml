name: Benchmark no-op workflow
enabled: true
triggers:
  - type: scheduled
    with:
      every: 1m
steps:
  - name: no-op
    type: console
    with:
      message: 'No-op workflow executed at {{ now }}'
