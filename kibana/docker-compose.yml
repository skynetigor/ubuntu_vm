services:
  kibana-dev:
    build:
      context: .
      args:
        KIBANA_REPO_URL: https://github.com/elastic/kibana
        KIBANA_BRANCH: main
    ports:
      - "5011:5601"   # Kibana
      - "5012:9200"   # Elasticsearch
    volumes:
      - es_snapshots:/root/.es         # downloaded ES snapshot cache
      - ./kibana.dev.yml:/etc/kibana-config/kibana.dev.yml:ro
    restart: unless-stopped

volumes:
  es_snapshots:
