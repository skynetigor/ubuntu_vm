outputs:
  default:
    type: elasticsearch
    hosts: ['http://elasticsearch:9200']
    username: elastic
    password: ${ES_PASSWORD}

inputs:
  - type: apm
    id: apm-1
    apm-server:
      host: "0.0.0.0:8200"
      rum:
        enabled: false
