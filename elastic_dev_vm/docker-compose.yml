services:
  dev-vm:
    build: .
    container_name: elastic-dev-vm
    privileged: true
    ports:
      - "3500:22"
    volumes:
      - D:\DOCKER_FILES\elastic-dev-vm\home:/root
    tmpfs:
      - /ramdisk:size=10g
    restart: unless-stopped
